import Link from 'next/link'
import { CalendarPlus, CheckCircle2, MessageCircle } from 'lucide-react'
import { Button } from '@/components/ui/button'
import { Card, CardContent } from '@/components/ui/card'
import { formatINR } from '@/lib/utils'
export default async function BookingConfirmationPage({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; return <main className="mx-auto max-w-3xl px-4 py-16 md:px-6"><Card><CardContent className="p-8 text-center md:p-12"><div className="mx-auto grid size-16 place-items-center rounded-full bg-primary/10 text-primary"><CheckCircle2 className="size-9" /></div><p className="mt-5 text-sm font-semibold uppercase tracking-[.18em] text-primary">Booking confirmed</p><h1 className="mt-2 text-4xl font-black">You’re booked.</h1><p className="mt-3 text-muted-foreground">Booking ID: <span className="font-semibold text-foreground">{id}</span></p><div className="mx-auto mt-8 max-w-lg rounded-2xl border bg-muted/50 p-5 text-left"><p className="font-bold">Your stay</p><p className="mt-1 text-sm text-muted-foreground">Mizoram</p><div className="mt-5 grid grid-cols-2 gap-4 text-sm"><div><p className="text-muted-foreground">Check-in</p><p className="font-semibold">12 Oct 2026</p></div><div><p className="text-muted-foreground">Check-out</p><p className="font-semibold">16 Oct 2026</p></div><div><p className="text-muted-foreground">Guests</p><p className="font-semibold">2 adults</p></div><div><p className="text-muted-foreground">Total paid</p><p className="font-semibold">{formatINR(7920)}</p></div></div></div><div className="mt-7 flex flex-col gap-2 sm:flex-row sm:justify-center"><Link href="/account"><Button>View booking</Button></Link><Button variant="outline"><MessageCircle className="size-4" />Contact host</Button><Button variant="outline"><CalendarPlus className="size-4" />Add to calendar</Button></div></CardContent></Card></main> }
