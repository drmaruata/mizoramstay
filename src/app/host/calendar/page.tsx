import { CalendarDays, ChevronLeft, ChevronRight } from 'lucide-react'
import { Button } from '@/components/ui/button'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
import { PortalShell } from '@/components/host/portal-shell'
export default function HostCalendarPage(){const days=Array.from({length:30},(_,i)=>i+1);return <PortalShell><div className="flex items-center justify-between"><div><p className="text-sm text-muted-foreground">Inventory</p><h1 className="mt-1 text-3xl font-black">October 2026</h1></div><div className="flex gap-2"><Button variant="outline" size="sm"><ChevronLeft className="size-4"/></Button><Button variant="outline" size="sm"><ChevronRight className="size-4"/></Button></div></div><Card className="mt-6"><CardHeader><CardTitle className="flex items-center gap-2"><CalendarDays className="size-5 text-primary"/>Room availability</CardTitle></CardHeader><CardContent><div className="mb-4 flex flex-wrap gap-3 text-xs text-muted-foreground"><span className="flex items-center gap-2"><i className="size-3 rounded bg-primary/20"/>Available</span><span className="flex items-center gap-2"><i className="size-3 rounded bg-accent/60"/>Booked</span><span className="flex items-center gap-2"><i className="size-3 rounded bg-foreground/10"/>Blocked</span></div><div className="overflow-auto"><div className="min-w-[820px]"><div className="grid grid-cols-[170px_repeat(30,minmax(28px,1fr))] text-xs"><div className="border-b p-2 font-semibold">Room</div>{days.map((d)=><div key={d} className="border-b border-l p-2 text-center text-muted-foreground">{d}</div>)}{[['Deluxe Double Room',1],['Family Room',2]].map(([room,row])=><div key={String(room)} className="contents"><div className="border-b p-3 font-semibold">{room}</div>{days.map((d)=><div key={`${row}-${d}`} className={`border-b border-l p-1 ${[7,8,14,15,22].includes(d)?'bg-accent/30':'bg-primary/5'}`}><div className="h-7 rounded-md" /></div>)}</div>)}</div></div></div></CardContent></Card></PortalShell>}
